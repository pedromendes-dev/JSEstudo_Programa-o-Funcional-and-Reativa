class Mainlibrary {
    #subcribers = [];                // # indica que é um campo privado de classe, só pode ser avessado dentro da própria classe e não fora dela.

    addNewBokk(book){
        this.#subcribers.forEach((sub) => {                 // this = "este objeto aqui"

        })                 
    }
}





() => {
    return a + b;
}

(2, 3)